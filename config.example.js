// Réglages de l'app. Copier ce fichier en `config.js` et renseigner les valeurs.
// La clé doit être identique à celle des nœuds Code du workflow n8n.
window.DTT_CONFIG = {
  apiBase: 'https://exemple.n8n.cloud/webhook',
  apiKey: 'REMPLACER_PAR_VOTRE_CLE',
  allowedDomains: ['cryoport.com'],
  historyLimit: 60
};
